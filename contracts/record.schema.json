{
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "$id": "https://arogyax.local/contracts/record.schema.json",
 "title": "ArogyaX screening record",
 "description": "One doorstep screening. Shared verbatim by the app's local sqlite store, the sync payload, and the PHC dashboard. Locked at H0 as v1; v2 gave the PPG/fusion layer and the PHC referral acknowledgement a home; v3 added the clinician outcome (the finding, distinct from referralState which is process); v4 adds age band and geography for the longitudinal risk engine. See contracts/sync.md for the wire protocol.",
 "type": "object",
 "required": [
  "recordId",
  "schemaVersion",
  "patientPseudoId",
  "whvId",
  "capturedAt",
  "ageBand",
  "villageCode",
  "tier",
  "modelVersion",
  "syncState"
 ],
 "additionalProperties": false,
 "properties": {
  "recordId": {
   "type": "string",
   "description": "UUID v4 generated on the phone. The sync is idempotent on this key, so a retried upload can never create a duplicate referral.",
   "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
  },
  "schemaVersion": {
   "type": "integer",
   "const": 4,
   "description": "v1 -> v2 added the PPG/fusion evidence fields and the referral acknowledgement block. v2 -> v3 added clinicianOutcome (this field was previously in the sqlite migration but not reflected here - drift fixed alongside the v4 bump). v3 -> v4 added ageBand and villageCode (required) plus sex/systolicBp/diastolicBp/glucose (optional). Every added field is nullable or has a migration default, so an older record is a valid newer record once this integer is bumped."
  },
  "patientPseudoId": {
   "type": "string",
   "description": "Salted hash of the MTM patient identifier. No name, no phone number, no Aadhaar ever leaves the device. The PHC re-identifies from its own MTM roll.",
   "minLength": 8
  },
  "whvId": {
   "type": "string",
   "description": "Women Health Volunteer identifier"
  },
  "phcId": {
   "type": "string",
   "description": "Destination Primary Health Centre"
  },
  "capturedAt": {
   "type": "string",
   "format": "date-time",
   "description": "ISO 8601 with timezone offset"
  },
  "ageBand": {
   "type": "string",
   "enum": [
    "45-54",
    "55-64",
    "65-74",
    "75+"
   ],
   "description": "REQUIRED. A band, not a date of birth or exact age. MTM screens 45+ (docs/PRODUCT.md section 2), and the risk engine needs age as a broad input, not as a precise one - an exact age plus villageCode plus sex would often uniquely identify a person in a small village, and clinical fields are stored unencrypted by design (docs/PRODUCT.md section 11). Choosing a band is the privacy decision, not an implementation shortcut."
  },
  "villageCode": {
   "type": "string",
   "description": "REQUIRED. An opaque village/locality code assigned by the district health authority - not a free-text place name, and not the same thing as lat/lon (which is the screening's own GPS fix, may be absent, and identifies a point rather than a place). This is what the district risk map groups by; it must not be joinable back to a name without the district's own gazetteer.",
   "minLength": 1
  },
  "sex": {
   "type": [
    "string",
    "null"
   ],
   "enum": [
    "female",
    "male",
    "other",
    null
   ],
   "description": "Optional. Filled when the MTM workflow has already collected it; never asked as a new question for this screening alone."
  },
  "systolicBp": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 50,
   "maximum": 300,
   "description": "Optional, mmHg. From the same MTM visit's existing BP check (docs/PRODUCT.md section 2), not a new measurement this product takes."
  },
  "diastolicBp": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 30,
   "maximum": 200,
   "description": "Optional, mmHg. See systolicBp."
  },
  "glucose": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 0,
   "description": "Optional, mg/dL, random or fasting unspecified. From the same MTM visit's existing glucose check, where available (docs/PRODUCT.md section 2)."
  },
  "lat": {
   "type": [
    "number",
    "null"
   ],
   "minimum": -90,
   "maximum": 90
  },
  "lon": {
   "type": [
    "number",
    "null"
   ],
   "minimum": -180,
   "maximum": 180
  },
  "locationAccuracyM": {
   "type": [
    "number",
    "null"
   ],
   "description": "null when GPS was unavailable; the record is still valid"
  },
  "ppgResult": {
   "type": [
    "string",
    "null"
   ],
   "enum": [
    "regular",
    "irregular",
    "unclear",
    "skipped",
    null
   ],
   "description": "Camera pre-screen outcome. 'irregular' or 'unclear' escalates to ECG."
  },
  "ppgMeanHr": {
   "type": [
    "number",
    "null"
   ]
  },
  "ppgIrregularityScore": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 0,
   "maximum": 1,
   "description": "Contact-PPG interval irregularity, on the same scale as rrIrregularityScore by construction (both come from RrAnalyser)."
  },
  "ppgPerfusionIndex": {
   "type": [
    "number",
    "null"
   ],
   "description": "Perfusion index (%). Below PpgAnalyser.kPerfusionGate the PPG was discarded and every fusion field below is null."
  },
  "ecgDurationSec": {
   "type": "number",
   "minimum": 0
  },
  "sqiScore": {
   "type": "number",
   "minimum": 0,
   "maximum": 1,
   "description": "0-1 signal quality. Below the gate the record is RETAKE and carries no scores."
  },
  "motionRejected": {
   "type": "boolean",
   "description": "true if the motion gate or a BLE seq gap invalidated the window"
  },
  "leadOffDetected": {
   "type": "boolean"
  },
  "meanHr": {
   "type": [
    "number",
    "null"
   ]
  },
  "rrIntervalCount": {
   "type": [
    "integer",
    "null"
   ]
  },
  "rrIrregularityScore": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 0,
   "maximum": 1,
   "description": "Rule-based RR-irregularity output. Always present when the signal passed the gates."
  },
  "cnnScore": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 0,
   "maximum": 1,
   "description": "INT8 CNN output. null when the CNN did not run."
  },
  "decidedBy": {
   "type": "string",
   "enum": [
    "rules",
    "cnn",
    "rules+cnn",
    "gate"
   ],
   "description": "Which path produced the tier. 'gate' means RETAKE."
  },
  "pulseDeficitBpm": {
   "type": [
    "number",
    "null"
   ],
   "description": "ECG heart rate minus PPG pulse rate over the same window. A beat too early to fill the ventricle never reaches the finger, so the electrical rate exceeds the mechanical one. Null unless ECG and PPG were captured simultaneously on the ESP32 clock."
  },
  "perfusedBeatFraction": {
   "type": [
    "number",
    "null"
   ],
   "minimum": 0,
   "maximum": 1,
   "description": "Fraction of R peaks followed by a pulse inside the transit-time window."
  },
  "nonPerfusingBeats": {
   "type": [
    "integer",
    "null"
   ],
   "minimum": 0
  },
  "medianPttMs": {
   "type": [
    "number",
    "null"
   ],
   "description": "Median pulse transit time across matched beats. Recorded, not used in the decision."
  },
  "fusionValid": {
   "type": "boolean",
   "default": false,
   "description": "False when the two captures were not simultaneous or either side was unusable. When false, every fusion field above is meaningless and must be ignored."
  },
  "fusionImplausible": {
   "type": "boolean",
   "default": false,
   "description": "The deficit came out negative, which is physiologically impossible and indicts the R-peak detector rather than the patient."
  },
  "ppgCorroboration": {
   "type": [
    "string",
    "null"
   ],
   "enum": [
    "none",
    "unusable",
    "agreed",
    "pulseDeficit",
    "nonPerfusingBeats",
    null
   ],
   "description": "What the contact PPG contributed. Mirrors PpgCorroboration in app/lib/core/policy.dart. This is the field that makes an AMBER-to-RED escalation auditable instead of mysterious: without it a PHC sees a RED it cannot explain."
  },
  "tier": {
   "type": "string",
   "enum": [
    "RED",
    "AMBER",
    "GREEN",
    "RETAKE"
   ]
  },
  "modelVersion": {
   "type": "string",
   "description": "e.g. 'rules-1.0' or 'af-cnn-int8-1.0+cal2'. The calibration used is part of the version, because the threshold is part of the model."
  },
  "ecgWaveformRef": {
   "type": [
    "string",
    "null"
   ],
   "description": "Local file path to the raw trace. Not uploaded in the hackathon build."
  },
  "syncState": {
   "type": "string",
   "enum": [
    "pending",
    "synced",
    "failed"
   ],
   "description": "Device-owned. The upload is only marked 'synced' on a per-record server ack, never on an HTTP 200 for the enclosing batch."
  },
  "syncedAt": {
   "type": [
    "string",
    "null"
   ],
   "format": "date-time"
  },
  "referralState": {
   "type": [
    "string",
    "null"
   ],
   "enum": [
    "none",
    "acknowledged",
    "patient_contacted",
    "visit_scheduled",
    "seen_at_phc",
    "closed",
    null
   ],
   "description": "SERVER-OWNED. Set by the PHC dashboard and pulled back to the worker's phone, so the WHV can see which of her referrals were acted on. A closed enum rather than a free-text note on purpose: worker-facing text must come from the static string table (CLAUDE.md non-negotiable 6), and an unreviewed free-text channel is exactly how a diagnosis word would reach the worker's screen (non-negotiable 1). The device must never include this field in an upload."
  },
  "referralUpdatedAt": {
   "type": [
    "string",
    "null"
   ],
   "format": "date-time"
  },
  "referralUpdatedBy": {
   "type": [
    "string",
    "null"
   ],
   "description": "PHC staff identifier. Not a patient identifier and not free text."
  },
  "clinicianOutcome": {
   "type": [
    "string",
    "null"
   ],
   "enum": [
    "confirmed",
    "not_confirmed",
    "inconclusive",
    null
   ],
   "description": "SERVER-OWNED. What the PHC clinician found after seeing a referred patient. Set only by the dashboard and pulled back through GET /v1/acks. A closed enum, never free text and never a condition name: this is the only channel by which a clinical finding reaches a worker's phone, and an unreviewed text field here is exactly how the words this product must never display would get onto her screen. 'confirmed' means the clinician confirmed the rhythm finding the referral was raised for; it is the training label that makes model improvement possible at all, since referralState only records process."
  },
  "clinicianOutcomeAt": {
   "type": [
    "string",
    "null"
   ],
   "format": "date-time",
   "description": "SERVER-OWNED. When clinicianOutcome was set."
  }
 }
}
